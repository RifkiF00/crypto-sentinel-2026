# Dashboard runtime configuration
VITE_SENTINEL_API_URL=http://localhost:8000
VITE_CORE_API_URL=http://localhost:8080
# live never uses fixtures; demo uses explicitly labelled fixtures; hybrid is for controlled testing
VITE_APP_MODE=live
